<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Directorio de archivos internos de la aplicación -->
    <files-path name="files" path="." />
    
    <!-- Cache de la aplicación -->
    <cache-path name="cache" path="." />
    
    <!-- Directorio externo específico de la app -->
    <external-files-path name="external_files" path="." />
    
    <!-- Cache externo específico de la app -->
    <external-cache-path name="external_cache" path="." />
    
    <!-- Directorio Downloads específico de la app -->
    <external-files-path name="downloads" path="Downloads/" />
</paths>